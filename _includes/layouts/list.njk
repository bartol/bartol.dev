---
layout: page
collection: root
---
{% if collection === 'root' %}
is root
{% endif %}
<ul>
{%- for post in collections[collection] -%}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
{%- endfor -%}
</ul>
{{ content | safe }}
