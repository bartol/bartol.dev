# Disable ufw firewall

	$ sudo ufw disable
# Enable ufw firewall

	$ sudo ufw enable
# Reload ufw firewall

	$ sudo ufw reload
# Ufw allow service

	$ sudo ufw allow ssh
# Ufw deny service

	$ sudo ufw deny http
# View status of ufw firewall

	$ sudo ufw status verbose
